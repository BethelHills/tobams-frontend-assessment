/**
 * `sizes` for section images: account for mobile/tablet horizontal padding; full width on lg+.
 */
export const CONTENT_IMAGE_SIZES =
  "(max-width: 639px) calc(100vw - 2rem), (max-width: 1023px) calc(100vw - 3rem), 100vw";
