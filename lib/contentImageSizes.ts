/**
 * Responsive `sizes` for full-width content inside the main column (up to max-w-7xl / 90rem).
 */
export const CONTENT_IMAGE_SIZES =
  "(max-width: 639px) 100vw, (max-width: 1023px) min(100vw, 48rem), min(90rem, 100vw)";
